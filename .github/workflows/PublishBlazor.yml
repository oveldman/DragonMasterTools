# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_PublishBlazor --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: PublishBlazor

on:
  push:
    branches:
      - main

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './DragonMaster/build.cmd PublishBlazor'
        run: ./DragonMaster/build.cmd PublishBlazor
        env:
          DragonMasterBlazorPublishToken: ${{ secrets.DRAGON_MASTER_BLAZOR_PUBLISH_TOKEN }}
          MadWorldBlazorPublishToken: ${{ secrets.MAD_WORLD_BLAZOR_PUBLISH_TOKEN }}
